{
  "name": "falcon-axios",
  "version": "0.0.1",
  "keywords": [
    "axios",
    "cache",
    "abort",
    "retry"
  ],
  "author": "Holy-Mortal",
  "license": "ISC",
  "description": "Based on axios secondary encapsulation plug-in, interface caching, interface cancellation, interface disconnection reconnection functions are provided",
  "files": [
    "src"
  ],
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.6.2",
    "crypto-js": "^4.2.0"
  }
}
